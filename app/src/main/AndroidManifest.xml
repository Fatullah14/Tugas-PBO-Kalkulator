<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelompok4.PocketCalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pocket_calculator"
        android:label="Pocket Calculator"
        android:roundIcon="@mipmap/pocket_calculator_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.kelompok4.PocketCalculator.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>